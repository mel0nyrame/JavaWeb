# HTTP协议

`HTTP`:超文本传输协议(通常使用HTTP/1.1)

`HTTPS`:安全超文本传输协议

`HTTP`规定:

- 交互的方式
  - `REQUEST`(请求):客户端向服务端发送
  - `RESPONSE`(响应):服务端向客户端发送
- 数据的格式
  - 请求发送的数据称为请求报文
  - 响应发送的数据称为响应报文
- 报文格式:
  - 报文首部
    - 报文行
    - 报文头
    - 报文体

`HTTP`链接